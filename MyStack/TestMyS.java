package MyStack;

import java.util.ArrayList;
import java.util.Objects;

public class TestMyS implements MyStack{



    @Override
    public void push(ArrayList<Object> arr, Object x) {
        arr.add(x);
    }

    @Override
    public Object pop(ArrayList<Object> arr) {
        if(arr.isEmpty()) return null;
        Object tmp=arr.getLast();
        arr.removeLast();
        return tmp;
    }
    public static void main(String []args){
        ArrayList<Object> arr = new ArrayList<>();
        MyStack myStack = new TestMyS();
        myStack.push(arr,"dsajjdakf");
        myStack.push(arr,23423);
        for(int i=0;i<2;i++){
            System.out.println(myStack.pop(arr));
















































































































































































































































































































































































































































































































































































































































































































































        }
    }
}